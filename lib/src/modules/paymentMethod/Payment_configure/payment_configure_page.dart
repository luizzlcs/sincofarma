import 'package:flutter/material.dart';

class PaymentConfigurePage extends StatelessWidget {

  const PaymentConfigurePage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Selecionar tipo de cliente'),),
           body: Container(),
       );
  }
}