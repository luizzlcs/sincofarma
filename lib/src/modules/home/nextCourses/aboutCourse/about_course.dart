import 'package:flutter/material.dart';

class AboutCourse extends StatelessWidget {

  const AboutCourse({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Sobre o curso'),),
           body: Container(),
       );
  }
}