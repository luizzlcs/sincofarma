import 'package:flutter/material.dart';

class RegisterCoursePage extends StatelessWidget {

  const RegisterCoursePage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Selecione a forma de pagamento'),),
           body: Container(),
       );
  }
}