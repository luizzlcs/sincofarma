import 'package:flutter/material.dart';

class ConcludedPaymentPage extends StatelessWidget {

  const ConcludedPaymentPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Pagamento concluído'),),
           body: Container(),
       );
  }
}